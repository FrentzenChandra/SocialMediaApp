import {StyleSheet} from 'react-native';
import findFonts from '../../assets/fonts/helper/helper';

const style = StyleSheet.create({
  statsTextProfile: {
    fontFamily: findFonts('Inter', 900),
    fontSize: 20,
  },
});

export default style;
